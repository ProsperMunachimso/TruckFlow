info:
  name: Create Labour
  type: http
  seq: 17

http:
  method: POST
  url: "{{baseUrl}}/api/labour"
  body:
    type: json
    data: |-
      {
        "bookingId": "69e2a2396f9fd78421c0ac01",
        "type": "loading",
        "numberOfLabourers": 2,
        "hours": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
