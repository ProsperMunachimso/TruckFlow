info:
  name: Create Quote
  type: http
  seq: 14

http:
  method: POST
  url: "{{baseUrl}}/api/quotes"
  body:
    type: json
    data: |-
      {
        "bookingId": "69decdc65b7c5f06d5b321ce",
        "amount": 350,
        "estimatedDurationHours": 4,
        "notes": "Available on short notice"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
