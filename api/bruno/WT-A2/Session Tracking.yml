info:
  name: Session Tracking
  type: http
  seq: 25

http:
  method: GET
  url: "{{baseUrl}}/api/users/recent"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
