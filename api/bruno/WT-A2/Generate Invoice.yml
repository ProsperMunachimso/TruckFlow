info:
  name: Generate Invoice
  type: http
  seq: 21

http:
  method: POST
  url: "{{baseUrl}}/api/invoices"
  body:
    type: json
    data: |-
      {
        "bookingId": "69e2a2396f9fd78421c0ac01",
        "totalAmount": 350,
        "tax": 47.25
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
