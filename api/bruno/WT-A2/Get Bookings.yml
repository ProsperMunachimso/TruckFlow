info:
  name: Get Bookings
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/api/bookings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
