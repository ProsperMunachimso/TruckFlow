info:
  name: Register Transporter
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/users/register"
  body:
    type: json
    data: |-
      {
        "name": "Test Transporter",
        "email": "trans@test.com",
        "password": "123456",
        "role": "transporter",
        "vehicleDetails": {"truckType": "Flatbed", "capacityKg": 5000, "licensePlate": "01-D-12345"}
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
