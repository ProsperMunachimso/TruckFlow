info:
  name: Get Qoute
  type: http
  seq: 15

http:
  method: GET
  url: "{{baseUrl}}/api/quotes"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
