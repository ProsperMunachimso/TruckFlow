info:
  name: Delete User
  type: http
  seq: 26

http:
  method: DELETE
  url: "{{baseUrl}}/api/users/me"
  body:
    type: json
    data: '{"password": "123456"}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
