info:
  name: Get User
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/users/profile"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
