info:
  name: Get Labour
  type: http
  seq: 18

http:
  method: GET
  url: "{{baseUrl}}/api/labour"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
