info:
  name: Get Invoices
  type: http
  seq: 22

http:
  method: GET
  url: "{{baseUrl}}/api/invoices"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
